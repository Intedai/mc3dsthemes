# MC3DSTHEMES
Make 3ds themes in Minecraft

# Installation
make a virtual enviornment (venv) and then install the dependencies:  
`pip install .`  
after that run the program:  
`python -m mc3dsthemes.main`
